// Set this to your deployed backend's API URL when the frontend and
// backend are hosted separately (e.g. frontend on Vercel, backend on
// Render/Railway/Fly). Leave it null to use the same origin the page
// was loaded from — that's what you want when the Java server is also
// serving this frontend (e.g. running locally with `java -cp out com.campus.Main`).
//
// Example:
// window.SCHOLARIS_API_BASE = "https://scholaris-api.onrender.com/api";

window.SCHOLARIS_API_BASE = null;
